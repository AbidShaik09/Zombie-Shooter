using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadingSceneDefaultScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
        LoadScene.StartGame();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
